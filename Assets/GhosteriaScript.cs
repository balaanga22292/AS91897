using UnityEngine;

public class GhosteriaScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        gameObject.name = "Ghosteria";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
